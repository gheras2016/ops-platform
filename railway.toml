[build]
builder = "NIXPACKS"

[deploy]
# Run pending migrations on every deploy (idempotent) before serving, so schema
# changes (e.g. the subscriptions tables) are applied automatically on Railway.
startCommand = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"